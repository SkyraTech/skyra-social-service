# Skyra Social Service

Social media posting, analytics, and marketing automation service for LinkedIn, Twitter (X), and Meta platforms.

## Prerequisites
- Node.js (v18+ recommended)
- npm or yarn

## Setup
1. Run `npm install`.
2. Copy `.env.example` to `.env` and configure keys.
3. Run `npm start` to launch the service on port 8005.
